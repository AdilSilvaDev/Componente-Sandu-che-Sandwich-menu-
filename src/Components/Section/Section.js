const Section = ({ children }) => {
    // Retorna o JSX que representa o componente de seção
    return <section>{children}</section>; // Renderiza o conteúdo da seção
}

export default Section; // Exporta o componente de seção para uso em outros lugares do aplicativo