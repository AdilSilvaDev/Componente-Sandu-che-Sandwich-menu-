const Item = ({ children }) => {
    // Retorna o JSX que representa o componente de item da lista
    return <li>{children}</li>; // Renderiza o conteúdo do item da lista
}

export default Item; // Exporta o componente de item da lista para uso em outros lugares do aplicativo